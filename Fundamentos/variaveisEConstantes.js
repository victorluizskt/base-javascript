/*
 Com var podemos redeclara a mesma variavel duas vezes ou mais no mesmo programa.
*/

// Variável a recebe o valor 3
var a = 3;
var a = 12;

// Criar variaveis usando apenas let
let b = 4;
let s = 'String';

// Const podemos apenas declarar uma vez pois o valor é final
const c = 5;

console.log(a, b, c)
